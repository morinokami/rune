{
  "name": "rune-root",
  "version": "0.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/morinokami/rune.git"
  },
  "type": "module",
  "scripts": {
    "version": "changeset version && node scripts/update-example-versions.ts",
    "ready": "vp fmt && vp lint && ls-lint && knip && sherif && vp run test -r && vp run build -r",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@ls-lint/ls-lint": "catalog:",
    "@types/node": "catalog:",
    "knip": "catalog:",
    "publint": "catalog:",
    "sherif": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
