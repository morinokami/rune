{
  "name": "rune-root",
  "version": "0.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/morinokami/rune.git"
  },
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && ls-lint && knip && sherif && vp run --filter './packages/*' test && vp run --filter './packages/*' build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@ls-lint/ls-lint": "catalog:",
    "@types/node": "catalog:",
    "knip": "catalog:",
    "pkg-pr-new": "catalog:",
    "publint": "catalog:",
    "sherif": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
