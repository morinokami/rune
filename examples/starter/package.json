{
  "name": "my-cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "my-cli": "dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "start": "rune run",
    "build": "rune build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@rune-cli/rune": "^0.0.14",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
