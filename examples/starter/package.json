{
  "name": "my-cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "my-cli": "dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "start": "rune run",
    "build": "rune build",
    "test": "vitest run",
    "rune": "rune"
  },
  "devDependencies": {
    "@rune-cli/rune": "^0.0.30",
    "typescript": "^6.0.2",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
